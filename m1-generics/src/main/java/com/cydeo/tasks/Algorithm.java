package com.cydeo.tasks;

public class Algorithm {
    /*
    public static <T> T max(T x, T y){
        return (x > y) ? x : y;
    }

    code won't compile because ">" operator can not be applied to all generics, it is applicable only numeric types
     */

}
